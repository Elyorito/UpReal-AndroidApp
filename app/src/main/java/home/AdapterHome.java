package home;import android.app.Activity;import android.app.AlertDialog;import android.content.Context;import android.content.DialogInterface;import android.support.v7.widget.CardView;import android.support.v7.widget.RecyclerView;import android.view.LayoutInflater;import android.view.View;import android.view.ViewDebug;import android.view.ViewGroup;import android.widget.TextView;import android.widget.Toast;import com.upreal.upreal.R;import java.util.StringTokenizer;/** * Created by Elyo on 10/02/2015. */public class AdapterHome extends RecyclerView.Adapter<AdapterHome.ViewHolder> {    private AlertDialog.Builder builder;    private Context mContext;    private String mCardName[];    public static class ViewHolder extends RecyclerView.ViewHolder {        int holderId;        TextView text_cardHome;        CardView mCardView;        public ViewHolder(View itemView, int ViewType) {            super(itemView);            text_cardHome = (TextView) itemView.findViewById(R.id.text_cardview);            mCardView = (CardView) itemView.findViewById(R.id.cardview);        }    }    AdapterHome(String cardName[], Context context) {        this.mCardName = cardName;        this.mContext = context;        builder = new AlertDialog.Builder(this.mContext);        builder.setTitle("Voulez vous creer un compte?")                .setMessage("Pour acceder a cette fonctionnalite, vous devez creer un compte")                .setPositiveButton("Creer", new DialogInterface.OnClickListener() {                    @Override                    public void onClick(DialogInterface dialog, int which) {                        //Toast.makeText(, "Intent Create Account", Toast.LENGTH_SHORT);                    }                }).setNegativeButton("Annuler", new DialogInterface.OnClickListener() {            @Override            public void onClick(DialogInterface dialog, int which) {                dialog.cancel();            }        });    }    @Override    public AdapterHome.ViewHolder onCreateViewHolder(ViewGroup parent, int viewType) {        View v = LayoutInflater.from(parent.getContext()).inflate(R.layout.cardview_home, parent, false);        ViewHolder vhitem = new ViewHolder(v, viewType);        return vhitem;    }    @Override    public void onBindViewHolder(final AdapterHome.ViewHolder holder, final int position) {        holder.text_cardHome.setText(mCardName[position]);        holder.mCardView.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(final View v) {                switch (position) {                    case 0://Profile                        builder.create().show();                       Toast.makeText(v.getContext(), "[" +  mCardName[position] + "]", Toast.LENGTH_SHORT).show();                        return;                    case 1://Scan                        Toast.makeText(v.getContext(), "[" +  mCardName[position] + "]", Toast.LENGTH_SHORT).show();                        return;                    case 2://News                        Toast.makeText(v.getContext(), "[" +  mCardName[position] + "]", Toast.LENGTH_SHORT).show();                        return;                    case 3://LoyalCards                        builder.create().show();                        Toast.makeText(v.getContext(), "[" +  mCardName[position] + "]", Toast.LENGTH_SHORT).show();                        return;                    case 4://Lists                        builder.create().show();                        Toast.makeText(v.getContext(), "[" +  mCardName[position] + "]", Toast.LENGTH_SHORT).show();                        return;                    case 5://Acheivements                        builder.create().show();                        Toast.makeText(v.getContext(), "[" +  mCardName[position] + "]", Toast.LENGTH_SHORT).show();                        return;                }            }        });   }    @Override    public int getItemCount() {        return mCardName.length;    }}